# Configuration used for Spring Boot Tests.

# debug=true # verbose so should only be enabled for punctual spring context debugging purposes.
spring.datasource.url=jdbc:sqlite:./data/spring-test-sqlite-database.db
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
# Disabled cron
pokevax.enableCron=false